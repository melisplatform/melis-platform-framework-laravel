        $coreSrv = app('ZendServiceManager')->get('MelisCoreGeneralService');
        foreach ($data As $key => $val){

            foreach ($val->getOriginal() As $col => $row){
#TCTABLECOLS
            }
        }